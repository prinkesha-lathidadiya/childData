import { SerchUserPipe } from './serch-user.pipe';

describe('SerchUserPipe', () => {
  it('create an instance', () => {
    const pipe = new SerchUserPipe();
    expect(pipe).toBeTruthy();
  });
});
